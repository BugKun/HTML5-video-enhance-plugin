export default class core{
    constructor(node, options) {
        this.node = node;
        this.frame = 30;
        this.options = {
            ...options
        };
    }

    getFrame(){
        
    }
}
